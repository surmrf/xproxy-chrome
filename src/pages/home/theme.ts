import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#0b52cb',
      main: '#0b52cb',
      dark: '#002884',
      contrastText: '#fff',
    },
  },
});

export default theme;
